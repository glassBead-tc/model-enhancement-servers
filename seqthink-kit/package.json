{
  "name": "seqthink-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Sequentialthinking++ Kit monorepo",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter ./packages/* run build",
    "test": "pnpm -r --filter ./packages/* run test",
    "lint": "echo 'no lint in MVP'"
  }
}